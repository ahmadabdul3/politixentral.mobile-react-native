import { sectionTitle } from 'px/styles/typography';

const styles = {
  pageSection: {
    paddingTop: 10,
    paddingBottom: 10,
  },
  sectionTitle: {
    ...sectionTitle,
    paddingBottom: 0,
  },
  pageSectionContent: {

  },
};

export default styles;
